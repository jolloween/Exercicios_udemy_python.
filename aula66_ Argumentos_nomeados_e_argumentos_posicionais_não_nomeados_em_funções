"""
Argumentos nomeados e não nomeados em funções Python
Argumento nomeado tem nome com sinal de igual
Argumento não nomeado recebe apenas o argumento (valor)
"""

def soma(x, y, z): #parâmentros
    #definição
    print(f'{x=} y={y} {z=}', '|','x + y + z = ', x + y + z)

#argumentos execução da função
soma(3, 5, 4)#argumento posicional
soma(y=5, z=2, x=3) #Argumento nomeado
soma(3, 4, z=4)#foi nomeado apenas o z
# soma(3,y=4, 5) #Erro! se nomear antes do fim é obrigatório nome os argumentos a seguir

print(1, 2, 3, sep='-')#Separador
